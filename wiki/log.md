# 변경 이력

## [2026-07-27] ingest | 2026-07-25 정성호 법무부 장관 사의 표명 (원본 22건)

첫 ingest. 스키마 검증이 목적이었다.

**만든 페이지**
- `events/2026-07-25-정성호-사의` (손으로 작성)
- `people/이재명` `people/정성호` `people/한동훈` `people/박지원` `people/이준석` `people/정점식` (손으로 작성)
- `outlets/*` 25건 (`wiki:outlets` 스크립트 생성)
- `index.md`

**이재명 필터** — 통과. 제목에 「李」 2건, 「이재명 정부」 2건이 직접 등장했다.

**ingest 중 드러난 문제와 처리**

1. **매체 페이지에는 판단이 없다.** 전부 집계(보도율·평균 지연·사건별 프레임)뿐이라 LLM이 손으로 쓸 이유가 없었다. 손으로 쓰면 산수를 틀리고 매체가 늘수록 drift만 커진다.
   → `wiki:outlets` 스크립트로 분리. 인물·사건 페이지만 에이전트가 쓴다.

2. **깨진 링크.** 스크립트는 Firestore 슬러그(`2026-07-25-acedae`)를, 손으로 쓴 페이지는 읽히는 이름(`2026-07-25-정성호-사의`)을 썼다. 두 네임스페이스를 잇는 다리가 없었다.
   → `EventDoc.wikiSlug` 추가. 스크립트가 이 값을 쓴다.

3. **원본 제목 하나가 잘려 있다.** [[outlets/khan]] 의 제목이 「…“이재명 정부...」 로 끝난다. 네이버 검색 결과에서 잘린 채로 들어왔다. 사건 페이지에 명시했다.

**남긴 불확실성**
- 사건 발생 시각 06:50은 **추정**이다. 최초 관측 보도가 KBS 06:54라는 것만 확인됐다. 모든 지연 시간이 이 값에 달려 있다.
- [[outlets/mbc]] · [[outlets/ytn]] 의 '보도하지 않음' 은 질의어에 걸린 기사가 이 사건 기사가 아니어서 운영자가 제외한 결과다. 실제로 보도했는지는 확인되지 않았다.

**커밋하지 않음.** 사람이 `git diff` 로 검토한 뒤 커밋한다.
